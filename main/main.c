#include <stdio.h>
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "freertos/task.h"

void app_main(void)
{
    printf("EVIDENCE item=c3-day-16-task-notifications status=starter\n");
    /* TODO: Count deterministic events through one receiver's direct notification channel. */
    /* Emit status=pass only after every published acceptance check succeeds. */
}
